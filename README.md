# Gmail Calendar Agent

A small Streamlit prototype that reads sample emails and previews possible calendar events.

## Run the demo

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/streamlit run app.py
```

The first version uses local sample emails only. Gmail and Apple Calendar will be added in later steps.
